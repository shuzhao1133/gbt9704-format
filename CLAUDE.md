# gbt9704-format 开发须知

- 这是一个独立 git 仓库，是 `gbt9704-format` skill 的源码。**改这里的规则文件不会自动生效**——skill 缓存目录不持久，必须重新打包成 `.skill` 并重装，直接编辑源码会在下次同步时被还原。
- 当前定版：v2.4.0。

## 排版规范定版要点

### 版面与页边距
- 页边距：上 3.7 / 下 3.5 / 左 2.8 / 右 2.6 cm（A4，版心 156×225mm）

### 字体与字号
- 题目：方正小标宋简体 二号（22pt），不加粗，居中
- 章级标题（第X章/前言/目录等）：黑体 三号（16pt），不加粗，居中
- 一级标题（一、）：黑体 三号
- 二级标题（（一））：**楷体_GB2312** 三号，**不加粗**（覆盖规范图片"加粗"）
- 三/四级标题（1.、（1））：**仿宋_GB2312** 三号，不加粗
- 正文：**仿宋_GB2312** 三号，首行缩进 2 字符
- 数字与英文：Times New Roman

### 对齐与行距
- 行距：标题固定 32 磅，正文固定 28 磅，段前段后清零
- 对齐：**标题左对齐，正文两端对齐**（居中/右对齐的图题、落款保留）
- 缩进：**真标题顶格，正文与清单缩进 2 字符**（真标题/清单二分见 title-rules.md）

### 版式保护与规范
- 封面、目录不排页码；正文首页页码从 1 起算
- 页码：宋体 4 号"— N —"，距版心下缘 7mm，奇数页居右、偶数页居左各空一字
- 加粗、高亮、表格字体字号：**一律不动**，不参与格式统一；只清表格段落底纹
- 黑字白底：清除页面背景、段落底纹、高亮、彩字、下划线

### 文字格式硬伤修复（40+ 条规则）
- 全半角、空格、省略号、标点、序号写法、引号、书名号、连接号、发文字号、日期、百分数、千分位、附件说明等；详见 punctuation-rules.md
